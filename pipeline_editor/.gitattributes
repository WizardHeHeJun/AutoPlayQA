# 统一用 LF 入库，检出按平台默认；避免 Windows/CI 混合协作时整文件 diff
* text=auto

*.ps1 text eol=crlf
*.bat text eol=crlf
*.cmd text eol=crlf

# POSIX sh 钩子必须 LF（CRLF 会让 sh 解析失败）
*.sh text eol=lf

# 二进制文件不做换行转换
*.png binary
*.jpg binary
*.ico binary
*.zip binary
