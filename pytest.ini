[pytest]
# 仓库根一条 `pytest` 同时跑框架回归与 PipelineEditor 后端回归。
# 两套都不依赖真机（subprocess 一律 mock），可以合跑。
# 只跑其中一套时照旧显式给路径：`pytest tests -q` / `pytest pipeline_editor/tests -q`。
testpaths = tests pipeline_editor/tests
